{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c \"echo STARTING_PRISMA && npx prisma db push --skip-generate && echo STARTING_NODE && node --trace-uncaught --trace-warnings dist/src/index.js\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
